// Stored enums that are used across multiple versioned types
export enum AbookEntryDispositionStored {
	PLAYABLE_AUDIO = 0,
	COVER_IMAGE = 1,
}

export enum AbookEntrySourceTypeStored {
	UPLOAD = 0,
	URL = 1,
}

export enum BlobMetadataResultTypeStored {
	ERROR = 0,
	SUCCESS = 1,
}
